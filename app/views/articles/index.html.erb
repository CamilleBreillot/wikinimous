<h1>Wikinimous</h1>

<p><%= link_to "Create an article", new_article_path %></p>

<ul>
  <% @articles.each do |article| %>
    <li>
      <p><strong><%= link_to article.title, article_path(article.id) %></strong></p>
      <p><%= article.content %></p>
    </li>
  <% end %>
</ul>
